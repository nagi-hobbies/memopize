import 'package:flutter/material.dart';

class MemoPizeIconImage extends StatelessWidget {
  const MemoPizeIconImage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Image(image: AssetImage('assets/memopize_icon.png'));
  }
}
